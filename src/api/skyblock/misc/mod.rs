use serde::Deserialize;

#[derive(Deserialize, Debug)]
pub struct CollectedEggs {
    breakfast: usize,
    lunch: usize,
    dinner: usize,
    supper: usize,
    brunch: usize,
    dejeuner: usize,
}

#[derive(Deserialize, Debug)]
pub struct CollectedLocations {
    hub: Vec<String>,
    crystal_hollows: Vec<String>,
    mining_3: Vec<String>,
    mining_1: Vec<String>,
    farming_1: Vec<String>,
    combat_1: Vec<String>,
    foraging_1: Vec<String>,
    crimson_isle: Vec<String>,
    mining_2: Vec<String>,
    combat_3: Vec<String>,
    dungeon_hub: Vec<String>,
    fishing_1: Vec<String>,
}

#[derive(Deserialize, Debug)] 
pub struct Rabbits { // Oh Lord.
    collected_eggs: CollectedEggs,
    alice: usize,
    bob: usize,
    darla: usize,
    ginny: usize,
    guinness: usize,
    patch: usize,
    rosco: usize,
    almond_amaretto: usize,
    buttercream_blossom: usize,
    oakley: usize,
    collin: usize,
    aaron: usize,
    cocoa_comet: usize,
    bandit: usize,
    bronson: usize,
    ross: usize,
    huck: usize,
    blake: usize,
    benji: usize,
    dulce_drizzle: usize,
    riley: usize,
    petunia: usize,
    ricky: usize,
    bud: usize,
    espresso_eclair: usize,
    milly: usize,
    chewy: usize,
    angus: usize,
    fudge_fountain: usize,
    sargent: usize,
    zack: usize,
    mandy: usize,
    lenny: usize,
    fergie: usize,
    ginger_glaze: usize,
    forrest: usize,
    brian: usize,
    chester: usize,
    chompsky: usize,
    ryder: usize,
    honey_hazelnut: usize,
    icing_ivy: usize,
    hopper: usize,
    maui: usize,
    suri: usize,
    william: usize,
    marlow: usize,
    sweetpea: usize,
    demi: usize,
    brie: usize,
    augustus: usize,
    acker: usize,
    lily: usize,
    blossom: usize,
    breeze: usize,
    alpaca: usize,
    otto: usize,
    billy: usize,
    able: usize,
    patches: usize,
    porter: usize,
    bugsy: usize,
    morris: usize,
    lotte: usize,
    bertha: usize,
    jasmine_jello: usize,
    gadget: usize,
    fudge: usize,
    ollie: usize,
    pickles: usize,
    bugster: usize,
    ellie: usize,
    leopold: usize,
    ticky: usize,
    chase: usize,
    baloo: usize,
    mickey: usize,
    mona: usize,
    skippe: usize,
    sorbet: usize,
    cookie: usize,
    olivette: usize,
    peony: usize,
    indie: usize,
    barcode: usize,
    gee_gee: usize,
    jasmine: usize,
    alexander: usize,
    buffalo: usize,
    stuart: usize,
    ozwald: usize,
    aladdin: usize,
    scotch: usize,
    kiwi_kiss: usize,
    lavender_lemon: usize,
    maple_mirage: usize,
    gracie: usize,
    natalie: usize,
    cloudy: usize,
    goofy: usize,
    dash: usize,
    george: usize,
    remi: usize,
    bishop: usize,
    raven: usize,
    herbie: usize,
    domino: usize,
    thumper: usize,
    stanley: usize,
    iggy: usize,
    ruben: usize,
    rusty: usize,
    binky: usize,
    bubbles: usize,
    hope: usize,
    jake: usize,
    tornado: usize,
    copper: usize,
    niza: usize,
    bambam: usize,
    bramble: usize,
    peanut: usize,
    delboy: usize,
    thalai: usize,
    baxter: usize,
    sophie: usize,
    spot: usize,
    eastwood: usize,
    jynx: usize,
    sassy: usize,
    hondo: usize,
    rowdy: usize,
    smokey: usize,
    bun_bun: usize,
    olivier: usize,
    chip: usize,
    elvis: usize,
    poppy: usize,
    max: usize,
    vlad: usize,
    lone_ranger: usize,
    toby: usize,
    chomper: usize,
    theodore: usize,
    waffle: usize,
    annabelle: usize,
    bugs: usize,
    sniffles: usize,
    flip_flop: usize,
    chilli: usize,
    gouda: usize,
    maybelline: usize,
    snowball: usize,
    baby: usize,
    amazon: usize,
    scooter: usize,
    mochi: usize,
    demetrious: usize,
    cotton: usize,
    james: usize,
    snoppy: usize,
    louie: usize,
    pumpkin: usize,
    uncle_buck: usize,
    cotton_puff: usize,
    casper: usize,
    ringo: usize,
    francine: usize,
    hyde: usize,
    pretzel: usize,
    nougat_nebula: usize,
    dusty: usize,
    tagalong: usize,
    cuddles: usize,
    trixie: usize,
    pride: usize,
    delilah: usize,
    hefner: usize,
    sprinkles: usize,
    buckwheat: usize,
    comet: usize,
    gunther: usize,
    moody: usize,
    mopsy: usize,
    pancake: usize,
    brutus: usize,
    cajun: usize,
    rolf: usize,
    pepsi: usize,
    teddy: usize,
    blueberry: usize,
    oletta: usize,
    porsche: usize,
    asterix: usize,
    duchess: usize,
    gremlin: usize,
    jammer: usize,
    oreo: usize,
    mookie: usize,
    charmin: usize,
    basket: usize,
    frankie: usize,
    bruno: usize,
    bumper: usize,
    dalton: usize,
    arnie: usize,
    destiny: usize,
    cottontail: usize,
    paddington: usize,
    paddy: usize,
    ressie: usize,
    nutmeg: usize,
    quincy: usize,
    thor: usize,
    bindi: usize,
    pillsbury: usize,
    snuffy: usize,
    cassidy: usize,
    midnight: usize,
    ace: usize,
    candi: usize,
    kobi: usize,
    barney: usize,
    frank: usize,
    aloysius: usize,
    scout: usize,
    jazz: usize,
    razzie: usize,
    buster: usize,
    scuba: usize,
    orange_obsidian: usize,
    spooky: usize,
    figaro: usize,
    obelix: usize,
    pebbles: usize,
    claude: usize,
    dandelion: usize,
    demarcus: usize,
    wadsworth: usize,
    alexa: usize,
    miles: usize,
    gatsby: usize,
    bilbo: usize,
    morgan: usize,
    nibbles: usize,
    quentin: usize,
    niko: usize,
    audi: usize,
    theo: usize,
    bruce: usize,
    bagel: usize,
    zero: usize,
    snoopy: usize,
    stewart: usize,
    calypso: usize,
    jasper: usize,
    orlando: usize,
    crystal: usize,
    casanova: usize,
    easter: usize,
    humphrey: usize,
    gloria: usize,
    hadley: usize,
    brooks: usize,
    houdini: usize,
    tricky: usize,
    peppermint_pearl: usize,
    selene: usize,
    sunny: usize,
    popcorn: usize,
    ned: usize,
    jelly_bean: usize,
    beatrice: usize,
    wesson: usize,
    jazmin: usize,
    prince: usize,
    tobi: usize,
    stormy: usize,
    carter: usize,
    emerson: usize,
    spirit: usize,
    harley: usize,
    achilles: usize,
    ashes: usize,
    alfie: usize,
    bibsy: usize,
    cinnamon: usize,
    joey: usize,
    cadet: usize,
    sylvester: usize,
    gizmo: usize,
    phantom: usize,
    alpha: usize,
    beta: usize,
    delta: usize,
    epsilon: usize,
    gamma: usize,
    zeta: usize,
    butters: usize,
    collected_locations: CollectedLocations,
    void: usize,
    baldwin: usize,
    hop_a_long: usize,
    frodo: usize,
    harmony: usize,
    digger: usize,
    shadow: usize,
    olympe: usize,
    linus: usize,
    josephine: usize,
    kodo: usize,
    jonah: usize,
    inferno: usize,
    penny: usize,
    pinky: usize,
    quince_quark: usize,
    ginger: usize,
    necron: usize,
    archie: usize,
    mimi: usize,
    oliver: usize,
    storm: usize,
    neptune: usize,
    hunter: usize,
    caramel: usize,
    sven: usize,
    hubby: usize,
    widget: usize,
    una: usize,
    zombie: usize,
    barbie: usize,
    solomon: usize,
    sage: usize,
    fish: usize,
    woody: usize,
    fluffy: usize,
    arachno: usize,
    echo: usize,
    picky: usize,
    hugo: usize,
    reginald: usize,
    webb: usize,
    cave: usize,
    rupert: usize,
    callie: usize,
    prestige: usize,
    crush: usize,
    bartholomew: usize,
    eta: usize,
    iota: usize,
    theta: usize,
    lulu: usize,
    penelope: usize,
    favor: usize,
    fitch: usize,
    pepper: usize,
    kappa: usize,
    rambo: usize,
    blackberry: usize,
    fievel: usize,
    ella: usize,
    cricket: usize,
    hershey: usize,
    punch: usize,
    cupcake: usize,
    chevy: usize,
    average: usize,
    cooper: usize,
    raspberry_ripple: usize,
    jade: usize,
    modest: usize,
    spencer: usize,
    ember: usize,
    prudent: usize,
    squish: usize,
    atlas: usize,
    seinfield: usize,
    jedi: usize,
    chubby: usize,
    abigail: usize,
    turbo: usize,
    apollo: usize,
    draco: usize,
    polka_dot: usize,
    kiera: usize,
    murphy: usize,
    badger: usize,
    bugatti: usize,
    stinky: usize,
    milo: usize,
    einstein: usize,
    ken: usize,
    heidie: usize,
    monalisa: usize,
    molly: usize,
    zea: usize,
    kaeman: usize,
    jeffery: usize,
    blackjack: usize,
    el_dorado: usize,
    winston: usize,
    trix: usize,
    irena: usize,
    mystic: usize,
    lambda: usize,
    willow: usize,
    cottonball: usize,
    dallas: usize,
    magic: usize,
    burst: usize,
    merlin: usize,
    mediocrity: usize,
    fuzzy: usize,
    general: usize,
    angel: usize,
    mu: usize,
    strawberry_swirl: usize,
    alpine: usize,
    saga: usize,
    galaxy: usize,
    honey: usize,
    king: usize,
    jerry: usize,
    toffee_tulip: usize,
    implode: usize,
    zorro: usize,
    abi: usize,
    river: usize,
    onyx: usize,
    ube_unicorn: usize,
    simba: usize,
    nu: usize,
    vanilla_vortex: usize,
    xi: usize,
    omicron: usize,
    napoleon: usize,
    nova: usize,
    pi: usize,
    walnut_whirl: usize,
    april: usize,
    rho: usize,
    sigma: usize,
    xoco_xanudu: usize,
    tau: usize,
    upsilon: usize,
    yogurt_yucca: usize,
    dante: usize,
    hole: usize,
    octavia: usize,
    soon: usize,
    krys: usize,
    pearl: usize,
    gravler: usize,
    carmine: usize,
    dojo: usize,
    ender: usize,
    grog: usize,
    mite: usize,
    violet: usize,
    carrot: usize,
    morty: usize,
    canopus: usize,
    aurora: usize,
    egg: usize,
    kronk: usize,
    brick: usize,
    boulder: usize,
    kalumdai: usize,
    zest_zephyr: usize,
    towny: usize,
    phi: usize,
    eisen: usize,
    attila: usize,
    mutonio: usize,
    melon: usize,
    striker: usize,
    extra: usize,
    albus: usize,
    oakly: usize,
    chi: usize,
    celestia: usize,
    mush_mush: usize,
    sulfur: usize,
    jacqueline: usize,
    psi: usize,
    coltron: usize,
    potato: usize,
    ruina: usize,
    omega: usize,
    harland: usize,
    halmenrog: usize,
    orchid: usize,
    emma: usize,
}

#[derive(Deserialize, Debug)]
pub struct TimeTower {
    charges: usize,
    activation_time: usize,
    level: usize,
}

#[derive(Deserialize, Debug)]
pub struct Employee {
    rabbit_bro: usize,
    rabbit_cousin: usize,
    rabbit_sis: usize,
    rabbit_father: usize,
    rabbit_grandma: usize,
    rabbit_uncle: usize,
    rabbit_dog: usize,
}

#[derive(Deserialize, Debug)]
pub struct Shop {
    year: usize,
    rabbits: Vec<String>,
    chocolate_spent: usize,
    cocoa_fortune_upgrades: usize,
}

#[derive(Deserialize, Debug)]
pub struct RabbitHitmen {
    rabbit_hitmen_slots: usize,
    missed_uncollected_eggs: usize,
    egg_slot_cooldown_mark: usize,
    egg_slot_cooldown_sum: usize,
}

#[derive(Deserialize, Debug)]
pub struct Easter {
    rabbits: Rabbits,
    time_tower: TimeTower,
    employees: Employee,
    chocolate: usize,
    total_chocolate: usize,
    chocolate_since_prestige: usize,
    last_viewed_chocolate_factory: usize,
    shop: Shop,
    rabbit_barn_capacity_level: usize,
    rabbit_sort: String,
    chocolate_level: usize,
    rabbit_filter: String,
    chocolate_multiplier_upgrades: usize,
    rabbit_rarity_upgrades: usize,
    golden_click_amount: usize,
    golden_click_year: usize,
    el_dorado_progress: usize,
    supreme_chocolate_bars: usize,
    click_upgrades: usize,
    refined_dark_cacao_truffles: usize,
    rabbit_hitmen: RabbitHitmen,
    #[serde(rename = "rabbit_hotspot_filer")]
    rabbit_hotspot_filter: String,
}

#[derive(Debug, Deserialize)]
pub struct Events {
    easter: Easter,
}
